{
  "name": "mastersrunners",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm -r run build",
    "build:web": "pnpm --filter @masters/web build",
    "task:intake": "node scripts/task-intake.mjs",
    "task:start": "node scripts/task-start.mjs",
    "task:resume": "node scripts/task-resume.mjs",
    "task:status": "node scripts/task-status.mjs",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm --filter @masters/database db:generate && pnpm --filter @masters/database exec tsc -p tsconfig.json --noEmit && pnpm --filter @masters/types exec tsc -p tsconfig.json --noEmit && pnpm --filter @masters/api exec tsc -p tsconfig.build.json --noEmit && pnpm --filter @masters/web exec tsc -p tsconfig.json --noEmit",
    "knip": "bash scripts/run-knip.sh",
    "format": "prettier . --write",
    "format:check": "prettier --check --ignore-unknown AGENTS.md README.md DEPLOYMENT.md design docs/README.md docs/guides docs/runbooks docs/reports/README.md docs/plans/README.md tasks scripts .github package.json",
    "precommit": "bash scripts/pre-commit.sh",
    "commitmsg:check": "commitlint",
    "prepare": "node scripts/run-if-bin.mjs husky",
    "ci:local": "bash scripts/ci-local.sh",
    "depcruise": "dependency-cruiser --config .dependency-cruiser.cjs apps packages",
    "deploy:verify": "bash scripts/verify-deployment.sh",
    "db:migrate": "pnpm --filter @masters/database db:migrate",
    "db:push": "pnpm --filter @masters/database db:push",
    "db:studio": "pnpm --filter @masters/database db:studio",
    "db:generate": "pnpm --filter @masters/database db:generate",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.58.2",
    "dependency-cruiser": "^17.3.9",
    "eslint": "^10.1.0",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-simple-import-sort": "^12.1.1",
    "globals": "^17.4.0",
    "husky": "^9.1.7",
    "knip": "^6.0.2",
    "prettier": "^3.8.1",
    "typescript": "^5",
    "typescript-eslint": "^8.57.1"
  },
  "packageManager": "pnpm@10.28.2"
}
