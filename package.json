{
  "name": "mastersrunners",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm --filter @masters/types exec tsc -p tsconfig.json --noEmit && pnpm --filter @masters/web exec tsc -p tsconfig.json --noEmit",
    "format": "prettier . --write",
    "format:check": "prettier --check --ignore-unknown AGENTS.md README.md DEPLOYMENT.md design docs/checklists docs/README.md docs/guides docs/runbooks docs/reports/README.md docs/plans/README.md tasks scripts .github package.json",
    "precommit": "bash scripts/pre-commit.sh",
    "commitmsg:check": "commitlint",
    "prepare": "husky",
    "ci:local": "bash scripts/ci-local.sh",
    "depcruise": "dependency-cruiser --config .dependency-cruiser.cjs apps packages",
    "deploy:verify": "bash scripts/verify-deployment.sh",
    "db:migrate": "pnpm --filter @masters/database db:migrate",
    "db:push": "pnpm --filter @masters/database db:push",
    "db:studio": "pnpm --filter @masters/database db:studio",
    "db:generate": "pnpm --filter @masters/database db:generate",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.3",
    "@commitlint/config-conventional": "^20.4.3",
    "@eslint/js": "^9.39.2",
    "@playwright/test": "^1.58.2",
    "dependency-cruiser": "^17.3.8",
    "eslint": "^9.39.2",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-simple-import-sort": "^12.1.1",
    "globals": "^17.4.0",
    "husky": "^9.1.7",
    "prettier": "^3.8.1",
    "typescript": "^5",
    "typescript-eslint": "^8.57.0"
  },
  "packageManager": "pnpm@10.28.2"
}
